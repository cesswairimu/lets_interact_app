
<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Joined</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <% @users.each do |user| %>
        <tr>
            <td><%= user.username %></td>
            <td><%= user.email %></td>
            <td><%= "#{time_ago_in_words(user.created_at)} ago" %></td>
        </tr>
    <% end %>
</table>
